name: Build Signed APK

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Generate signing keystore
        run: |
          keytool -genkeypair -v \
            -keystore release.keystore \
            -alias sarfitkey \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass "SarfitBuild123!" \
            -keypass "SarfitBuild123!" \
            -dname "CN=SARFIT, OU=Dev, O=SARFIT, L=City, S=State, C=US"
      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 7.5

      - name: Build signed release APK
        run: gradle assembleRelease
        env:
          KEYSTORE_PASSWORD: SarfitBuild123!
          KEY_ALIAS: sarfitkey
          KEY_PASSWORD: SarfitBuild123!

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SARFIT-signed-apk
          path: app/build/outputs/apk/release/app-release.apk

      - name: Upload keystore (keep this safe!)
        uses: actions/upload-artifact@v4
        with:
          name: SARFIT-keystore
          path: release.keystore
        
